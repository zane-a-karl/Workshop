main
var input, r;

function isEven(n);
var v, result;
{
    let v <- n;
    while v >= 2 do
        let v <- v - 2;
    od;

    if v == 2 then
        return 1;
    else
        return 0;
	  fi;
};

{
    let input <- call inputNum();
    let r <- call isEven(input);
    call outputNum(r);
}.
